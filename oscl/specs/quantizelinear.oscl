rules {
  inputs x, y_scale, y_zero_point;
  outputs y;

  result y.shape = shape(x);
  result y.type =
    if attribute("output_dtype", 0) != 0
    then attribute("output_dtype", 0)
    else input_type("y_zero_point", uint8);
}
