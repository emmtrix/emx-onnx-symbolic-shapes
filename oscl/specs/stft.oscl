shape {
  inputs signal, frame_step, window;
  outputs output;
  attributes onesided;

  let fs = shape_value(frame_step);

  result output = stft_shape(shape(signal), fs, shape(window), onesided);
}
