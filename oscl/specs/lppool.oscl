shape {
  inputs X;
  outputs Y;
  attributes kernel_shape, strides, pads, dilations, ceil_mode, auto_pad;

  result Y = pool_shape(shape(X), kernel_shape, strides, pads, dilations, ceil_mode, auto_pad);
}
