shape {
  inputs data, axes;
  outputs reduced;
  attributes keepdims, noop_with_empty_axes;

  let target_axes = shape_value(axes);

  result reduced = reduce_shape(shape(data), target_axes, keepdims);
}
