# Squeeze – Remove single-dimensional entries at the specified axes.
# The axes tensor supplies the positions to remove from the input shape.
shape {
  inputs data, axes;
  outputs squeezed;

  let target_axes = shape_value(axes);

  result squeezed = squeeze_shape(shape(data), target_axes);
}
