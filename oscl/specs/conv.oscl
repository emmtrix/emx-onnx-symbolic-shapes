shape {
  inputs X, W;
  outputs Y;
  attributes kernel_shape, strides, pads, dilations, group, auto_pad;

  result Y = conv_shape(shape(X), shape(W), kernel_shape, strides, pads, dilations, group, auto_pad);
}
