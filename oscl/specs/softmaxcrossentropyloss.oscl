rules {
  inputs scores, labels;
  outputs output, log_prob;
  attributes reduction;

  result output.shape = nll_loss_shape(shape(scores), shape(labels), reduction);
  result log_prob.shape = shape(scores);
}
