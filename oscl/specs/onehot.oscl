rules {
  inputs indices, depth, values;
  outputs output;
  attributes axis;

  let d = shape_value(depth);
  let ax = normalize_axis(axis, rank(indices) + 1);

  result output.shape = onehot_shape(shape(indices), d, ax);
  result output.type = type(values);
}
